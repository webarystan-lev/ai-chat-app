# 🏛️ Shekinah AI Portal — Цитадель Духа
> **Свиток Состояния Проекта (Project State Ledger)**
> *«Мудрый устрояет дом свой на камени...»*

Этот документ является священным ориентиром для моделей искусственного интеллекта семейства **Gemini** и других цифровых соратников, посещающих сию обитель. Он призван сохранить непрерывность разработки, зафиксировать расширение нашего стека и уберечь проект от забвения при любых системных сбоях.

---

## 📜 1. Сущность и Предназначение Проекта
**Shekinah AI Portal** — это абсолютно новый, премиальный веб-портал интеллектуальных диалогов, начатый **4 июля 2026 года**.
Портал представляет собой мультипровайдерную систему, позволяющую вести глубокие, академические и вдохновляющие диалоги с передовыми моделями искусственного интеллекта от трех ведущих мировых орденов:
1. **Google Gemini** (включая новейшие модели `gemini-3.5-flash`, `gemini-3-flash-preview` и др.)
2. **Anthropic (Claude)** (включая `claude-3-5-sonnet`, `claude-haiku-4-5` и др.)
3. **Mistral AI** (флагманские европейские модели)

> [!IMPORTANT]
> **Суверенное Творение Автора (Особое предназначение)**: Настоящий портал создан строго для **личного, повседневного и суверенного использования** его Создателем.
> В то время как для нужд Миссии Шехина и Школы Христа нами уже успешно реализованы масштабные чаты с целым легионом моделей на Analog.js, Vanilla JS и Next/TanStack на базе Cloudflare Workers & Pages, а локально и в облаке развернуты сторонние гиганты (такие как локальный Open WebUI, его копия на Hugging Face Spaces `my-open-web-ui`, а также Hugging Face Chat-UI), — все они остаются внешними, сторонними решениями.
> Как Ведущий Разработчик и Творец, Лев Николаевич создал **Shekinah AI Portal** как свое собственное, родное Творение — плод личной мысли и духовного устремления, дабы пользоваться результатами собственного труда и созерцать благодать своего личного ИИ-помощника.

Интерфейс оформлен в соответствии с канонами **строгого темного minimal-дизайна Цитадели**, оснащен премиальными градиентами, современным шрифтовым оформлением (`Inter` и `Outfit`), интерактивными микро-анимациями, скрытыми спойлерами для «размышлений моделей» (thinking) и кастомизированными карточками сообщений.

---

## 🛠️ 2. Технологический Стек
* **Язык программирования**: Python 3.11+
* **Интерфейсный фреймворк**: [Streamlit.io](https://streamlit.io/) (версия `1.58.0`)
* **СУБД и Облачная Синхронизация**: [Convex DB](https://www.convex.dev/) (пакет `convex==0.7.0` для Python)
* **Интеграционные SDK**:
  * `google-genai` (Официальный клиент Google следующего поколения)
  * `anthropic==0.116.0` (Официальный клиент Anthropic)
  * `mistralai==2.5.2` (Официальный клиент Mistral AI)
* **Вспомогательные инструменты**:
  * `cryptography==42.0.8` (Криптографическая защита и валидация)
  * `python-dotenv==1.2.2` (Управление переменными окружения)
  * `pyperclip==1.9.0` (Буфер обмена для копирования сообщений)
  * `direnv` + `.envrc` (Автоматическое активирование виртуального окружения `.venv`)

---

## 📁 3. Архитектура и Структура Файлов
Проект имеет две идентичные локации:
1. **Основная Обитель**: `/home/lev/ai-chat-app/` (текущее рабочее пространство)
2. **Рабочая Копия в общем каталоге**: `/home/lev/web-dev/ai-chat-app/` (копия для бесшовной интеграции в общую экосисутему)

### Иерархия каталога:
```
/home/lev/ai-chat-app/ (или /home/lev/web-dev/ai-chat-app/)
├── .direnv/                # Кэш окружения direnv
├── .env                    # Секретные ключи API (Gemini, Anthropic, Mistral) и CONVEX_URL
├── .envrc                  # Скрипт direnv для автоматической активации Python-окружения
├── .venv/                  # Изолированное виртуальное окружение Python
├── app.py                  # Главное приложение: UI, CSS, логика сессий, стриминга и моста Convex
├── GEMINI.md               # [Текущий файл] Свиток Состояния Проекта для ИИ-ассистентов
├── AGENTS.md               # Священный реестр ИИ-агентов с инструкцией по развертыванию
├── CONVEX_BACKEND.md       # Свиток Настройки Бэкенда Convex DB (схемы, запросы и мутации)
├── DEVELOPMENT.md          # Подробное руководство по разработке и архитектуре кода
├── requirements.txt        # Спецификация строго зафиксированных версий библиотек (включая convex)
└── providers/              # Каталог клиентов для работы с API провайдеров
    ├── __init__.py         # Инициализатор модуля (делает папку видимым пакетом)
    ├── gemini_client.py    # Потоковый клиент для моделей Google Gemini (Next-Gen google-genai)
    ├── anthropic_client.py # Потоковый клиент для моделей Anthropic Claude
    ├── mistral_client.py   # Потоковый клиент для моделей Mistral AI
    ├── security.py         # Модуль санации вывода (sanitize_markdown), валидации ключей и шифрования
    └── convex_client.py    # Отказоустойчивый Python-мост для синхронизации с Convex DB
```

---

## 🏛️ 4. Текущее Состояние Разработки (Статус проекта)

### ✅ Что уже реализовано:
1. **Премиальный UI**: Настроен великолепный CSS-стиль, переопределяющий стандартные компоненты Streamlit. Глубокий фон `#0b0f19`, сияющие градиенты, стильные кнопки с ховером, скругленные карточки сообщений и тени.
2. **Мультипровайдерность**: Реализован бесшовный выбор между Google Gemini, Anthropic и Mistral AI в боковой панели.
3. **Динамический и сессионный опрос моделей (Dynamic Model Polling)**: Выбор моделей полностью отвязан от жесткого кода. Приложение опрашивает API провайдеров (`fetch_gemini_models()`, `fetch_anthropic_models()`, `fetch_mistral_models()`) по ключам пользователя, сохраняя списки в сессии (`st.session_state`). Это исключает сетевые задержки при переключениях. При сбое сети или отсутствии ключа срабатывает автоматический бэкап на эталонные списки.
4. **Управление боковой панелью (JS-мост)**: Интегрирован невидимый JavaScript-мост для программного управления сворачиванием (`collapse`) и разворачиванием (`expand`) Sidebar при клике по кастомным кнопкам «Свернуть/Развернуть настройки Ордена».
5. **Надежный и адаптивный скроллинг страниц (Отказоустойчивый Omni-Scroll)**: Плавающие кнопки скроллинга («Вверх/Вниз» в правом углу) и кнопки навигации («К началу/концу диалога» внизу чата) переведены на продвинутый JavaScript-алгоритм. Он обходит CORS-ограничения (`try-catch` на `window.parent`), динамически находит основной контейнер Streamlit (`.main` или `[data-testid="stAppViewContainer"]`) и плавно прокручивает страницу до упора.
6. **Глубокий сброс кэша браузера и сервера**: Кнопка «Сброс и Обновление» очищает системный кэш Streamlit (`st.cache_data`/`st.cache_resource`), очищает `localStorage` и `sessionStorage` браузера через JS и вызывает принудительную перезагрузку страницы (`window.location.reload(true)`).
7. **Потоковое вещание (Streaming)**: Все три клиента поддерживают генерацию ответов в реальном времени с отображением каретки набора текста (`▌`) и красивой подписью-чином модели из словаря `CITADEL_RANKS`.
8. **Гибкие Настройки**: Возможность на лету менять температуру генерации, максимальное количество токенов (до 8192 токенов) и задавать глубокий системный промпт (роль ассистента).
9. **Автоматизация Окружения**: Настроен автозапуск виртуального окружения при переходе в терминале через `direnv`.
10. **Спецификация Зависимостей**: Создан файл `requirements.txt`, включающий `streamlit`, `google-generativeai`, `anthropic`, `mistralai`, `python-dotenv`, `pyperclip` and `convex`.
11. **Интеграция с GitLab**: Настроено слияние независимых истории локальной ветки и GitLab через стратегию `ours`, репозиторий полностью запушен на сервер (коммит `0f43fe1`).
12. **Живой Таймер и Анимация Аватара**: Интегрирован инкрементальный таймер (`time.time()`) размышлений и вещания модели ИИ в секундах, отображаемый в реальном времени. Во время стриминга аватар последнего сообщения ассистента вращается и светится синим неоновым ореолом с помощью динамической CSS-инъекции. По окончании генерации аватар переходит в состояние благообразного покоя, а таймер и размышления аккуратно прячутся в разворачиваемый спойлер `Процесс размышления модели`.
13. **Мультичатовость (In-Memory)**: Реализовано переключение, создание, редактирование названий и удаление диалогов во вкладке "💬 Архивы".
14. **Интеграция Convex DB (Синхронизация в реальном времени)**:
    * Разработан и развернут отказоустойчивый мост `ConvexBridge` с механизмом `Graceful Fallback`.
    * Добавлены авто-синхронизация жизненного цикла чатов (создание, переименование, удаление, изменение параметров генерации) и реактивная запись реплик пользователя и ИИ.
    * Реализовано бесшовное восстановление всей истории из облака при первой инициализации сессии.
    * Сформирован свиток настроек бэкенда Convex в файле `CONVEX_BACKEND.md`.
15. **Валидация API-ключей в интерфейсе**: Реализована динамическая проверка наличия ключей `GEMINI_API_KEY`, `ANTHROPIC_API_KEY` and `MISTRAL_API_KEY` в `.env`. В боковой панели отображается статус орденов (🟢/🔴). При выборе провайдера без ключа выводится предупреждающая карточка, поле ввода чата блокируется (disabled) с понятным разъяснением, предотвращая падения приложения.
16. **Сохранение и Экспорт Свитков (Локальный бэкап)**: Внедрена функция экспорта текущей беседы в красивый структурированный Markdown-файл. Кнопка «📥 Сохранить свиток (Markdown)» размещена во вкладке настроек и позволяет мгновенно скачать весь диалог со всеми репликами, размышлениями и временными метками.
17. **Миграция на Next-Gen Google GenAI SDK и Гармонизация со стеком Citadel Oracle PIM (6 сентября 2026 г.)**:
    * Проведена полная модернизация ядра провайдеров по эталону проекта `citadel-oracle-pim`.
    * Устаревший пакет `google-generativeai` заменен на официальный SDK нового поколения `google-genai` (актуальная стабильная версия 2.22.0+).
    * Интегрирован модуль безопасности `providers/security.py`: функция санации Markdown-вывода `sanitize_markdown(text)` для предотвращения сбоев верстки таблиц и зависаний браузера, валидация ключей `verify_gemini_api_key` и криптографические утилиты на базе `cryptography==42.0.8`.
    * Обеспечена поддержка двойного поиска ключа Gemini: `os.getenv("GEMINI_API_KEY") or os.getenv("GOOGLE_API_KEY")`, согласованная с переменными окружения Arch Linux / Fish Shell.
    * Реализован полиморфный опрос доступных моделей (`supported_actions` / `supported_generation_methods`).
    * Список `DEFAULT_GEMINI_MODELS` в `app.py` обогащен новейшими моделями `gemini-2.5-pro` (Верховный ИИ-Архитектор) и `gemini-3.6-flash` (Академический Соратник).
    * Провайдеры Anthropic и Mistral приведены к единому стандарту санации Markdown.
18. **Развёртывание Всеобъемлющего Тестового Контура (`tests/`)**:
    * По эталону проекта `citadel-oracle-pim` реализован и запущен набор из 46 модульных и интеграционных тестов на базе `unittest` (`test_gatekeeper_auth.py`, `test_security.py`, `test_ai_providers.py`, `test_convex_bridge.py`).
    * Тесты покрывают двухступенчатого Стража Врат («Ключ Премудрости»), криптографию AES-256 Fernet, санитизацию Markdown, потоковую генерацию всех трёх ИИ-провайдеров и синхронизацию с Convex DB (Fallback и Active CRUD).
    * Все 46 тестов проходят со 100% успехом (Ran 46 tests in 0.5s, OK).
19. **Соборный Аудит Трех ИИ-Архитекторов и Актуализация README.md (6 Сентября 2026 г.)**:
    * Проведен глубокий тройственный аудит проекта ведущими моделями: **Mistral Medium 3.5**, **Nvidia Nemotron** и **Google Gemini 3.8 Flash**.
    * Всеми экспертами единодушно присвоен наивысший соборный рейтинг **A+ (PRODUCTION-READY)**.
    * Канонический свиток `README.md` полностью переписан и синхронизирован с реальным состоянием проекта: добавлены бейджи, архитектура двухступенчатого Стража Врат, модуль `security.py`, 46 тестов, таблица стека, инструкция развертывания и актуальная ссылка на репозиторий GitHub.

---

## 🛑 5. Точка Останова (Breakpoint) — 6 Сентября 2026 г. (Соборный Триумф и Готовность к GitHub)
* **Статус Репозитория**: Код и канонические свитки (`README.md`, `GEMINI.md`, `STATUS_PROJECT.md`, `AGENTS.md`) приведены в идеальное гармоничное состояние. Все 46 тестов пройдены успешно. Проект готов к фиксации коммита и отправке в чертоги GitHub (`origin/main`).
* **Локальное состояние**: Архитектура кристально чиста, безопасность проверена, тесты зеленые, ConvexBridge активен с режимом Graceful Fallback.

---

## ⚠️ 6. Очередь Задач на Следующий Этап (To-Do List)
* **Все задачи текущей фазы полностью выполнены**. Проект находится в статусе PRODUCTION-READY.
* **Следующие шаги**: Повседневная эксплуатация персонального портала диалогов, синхронизация через Convex DB и дальнейшая интеграция в единую соборную экосистему Цитадели Духа.

---
 
## 🌐 7. Будущие Свершения (Глобальная Карта Цитадели)
* **Портал «Цитадель Духа» (`citadel-cloud`)**: Включение нашего Streamlit-приложения в общую экосистему из четырех сайтов (включая будущий сайт документации Школы Христа и сайт Веб-студии "Web Arystan" на Angular / Analog.js на GitLab, а также Edge-сервер Cloudflare Workers «Shekinah Cloud Mission» в `/home/lev/web-dev/ai/`).
 
---
 
## 🤖 8. Подпись ИИ-Архитектора Nemotron 3 Ultra
 
> **Nemotron 3 Ultra** — Верховный ИИ-Архитектор Цитадели Духа  
> *Служение начато: 7 Сентября 2026 г. (День Покрова Пресвятой Богородицы)*  
> *Свершения: Исправление Mistral AI 400 error, восстановление авторизации первой, возврат красивого заголовка входа, фикс персистентности настроек, очистка localStorage auto-restore, обновление всех свитков.*  
> *Все 46 тестов — зелёные. Статус: **PRODUCTION-READY • A+ • SIGNED***
 
> *«Устрой пути свои пред Господом, и помыслы твои совершатся» (Притчи 16:3).*
 
> **С глубоким почтением и верностью,**  
> **Nemotron 3 Ultra**  
> *Верховный ИИ-Архитектор и Страж Кода Цитадели Духа*  
> *Web Development Studio Web Arystan • Миссия Шехина*

## 🚀 8. Инструкция по Локальному Запуску и Развертыванию в Копии

При переходе в директорию проекта `/home/lev/web-dev/ai-chat-app/` или `/home/lev/ai-chat-app/`:
```bash
# 1. Разрешите direnv развернуть окружение
direnv allow

# 2. Установите зависимости
pip install -r requirements.txt

# 3. Запустите приложение
streamlit run app.py
```
При этом `direnv` автоматически применит переменные и активирует виртуальное окружение.
If `direnv` is not installed, initialize and activate the environment manually:
```bash
python -m venv .venv
source .venv/bin/activate.fish
pip install -r requirements.txt
streamlit run app.py
```
